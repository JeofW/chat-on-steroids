## 2.1.15 Jeof reliability maintenance build

- Carry upstream post-2.1.14 reliability work plus the focused prompt-readback fix from PR #401 and managed Core enrollment fixes from PR #384.
- Support the current ChatGPT shell's direct conversation mapping while treating connector display names only as untrusted presentation and confirming ownership through exact local MCP request IDs.
- Keep page-first request evidence pending until the matching request reaches local MCP ingress, preventing the first Core call from being filed under Unattributed activity solely because page evidence and local dispatch arrive in the opposite order.
- Reject new duplicate Secure Tunnel IDs across available Core, Desktop and Plugins connectors, contain legacy collisions at startup, and stop affected optional tunnels before replacements start during an ID swap.
- Use `JeofW/chat-on-steroids` for maintenance-fork releases, updater artifacts and companion-extension recovery while retaining the original app and connector identities for compatibility.

Reload the companion extension and refresh the affected CoS connectors in ChatGPT after updating.
